/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server Actions are enabled by default in Next.js 14
  // Vercel automatically handles the output configuration
}

module.exports = nextConfig

